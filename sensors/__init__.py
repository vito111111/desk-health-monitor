# -*- coding: utf-8 -*-
"""传感器插件层 —— 每个数据源独立写入统一健康状态契约(health_state)。

约定:
  · 自运行型源(手表/体脂秤/手柄这类独立轮询的) 继承 base.HealthSource, 跑自己的循环。
  · 推送型源(摄像头) 由宿主主循环每帧推送, 直接用 health_state.SourceWriter
    (见 ../pet_bridge.py), 不走本层。

新增一个源 = 在此目录加一个文件 + 跑起来, 桌宠/聚合端自动感知, 无需改它们一行代码。
"""
